// ? $grid-breakpoints: (
// ?     xs: 0,
// ?     sm: 576px,
// ?     md: 768px,
// ?     lg: 992px,
// ?    xl: 1200px,
// ?     xxl: 1400px
// ?   );

// ! NavBar properties in Bootstrap

// ? navbar-expand-lg - Make the navbar full width on large screens (≥1200px)
// ! navbar-expand-lg - Will not show the NavBar until the screen is at least 1200px wide
// ! [ apply to <nav class="navbar navbar-expand-lg"> ]
// ? navbar-expand-md - Make the navbar full width on medium screens (≥992px)
// ? navbar-expand-sm - Make the navbar full width on small screens (≥576px)

// ! background-color
// ? bg-dark - Dark background
// ? bg-light - Light background

// ! color (text)
// ? text-dark - Dark text
// ? text-light - Light text

// ? navbar-dark - Dark navbar and the text color is light

// ! navBar Responsive with Bootstrap

// ? navbar-expand-lg - Make the navbar full width on large screens (≥1200px) used in the <nav> element
// ? navbar-collapse - Collapse the navbar into a single line on small screens (≥576px) used in the <div> element
// ? collapse - Collapse the navbar used in the <div> element

// ? navbar-nav - A list of nav items used in the <ul> element
// ? nav-item - A single nav item used in the <li> element

// ! ms-auto - margin start auto used in the <ul> element
// ? ml-auto - margin left auto
// ? mr-auto - margin right auto

// ! Hamburger Menu in Bootstrap

// ! properties of the <button> element
// ! the button goes after the <a> title element
// ? class="navbar-toggler" - A button that toggles the display of the navbar on small screens (≥576px)
// ? type="button" - The type of the button
// ? data-bs-toggle="collapse" - The data-bs-toggle attribute is used to toggle the display of the navbar on small screens (≥576px)
// ? data-bs-target="#id of the collapse <div>" - The data-bs-target attribute is used to toggle the display of the navbar on small screens (≥576px)

// ! Flexbox in Bootstrap

// ? d-flex - Display the element as a flexbox
// ? flex-column - Display the element as a column
// ? flex-row - Display the element as a row
// ? flex-wrap - Display the element as a row and wrap the content
// ? justify-content-center - Center the content horizontally
// ? align-items-center - Center the content vertically
// ? flex-grow-1 - Increase the element's flex grow factor
// ? flex-shrink-1 - Decrease the element's flex shrink factor
// ? flex-basis-auto - Set the element's flex basis to auto

// ! d-sm-flex - Display the element as a flexbox on small screens (≥576px) line 51

// ! image in Bootstrap

// ? img-fluid - The image will scale to the full width of its container
// ? img-thumbnail - The image will scale to the width of its container
// ? img-rounded - The image will be rounded
// ? img-circle - The image will be circular

// ! margin in Bootstrap
// ? my-4 - margin top and bottom 4
// ? mx-auto - margin left and right auto

// ! display property in Bootstrap
// ? d-none - Display the element only on small screens (≥576px)
// ? d-sm-none - Display the element only on small screens (≥576px)
// ? d-md-none - Display the element only on medium screens (≥768px)
// ? d-lg-none - Display the element only on large screens (≥992px)
// ? d-xl-none - Display the element only on extra large screens (≥1200px)

// ? d-block - Display the element only on large screens (≥992px)
// ? d-sm-block - Display the element only on small screens (≥576px)
// ? d-md-block - Display the element only on medium screens (≥768px)
// ? d-lg-block - Display the element only on large screens (≥992px)
// ? d-xl-block - Display the element only on extra large screens (≥1200px)

// aria-label="Recipient's username"
// aria-describedby="button-addon2"

// ! g-4 - is a gap of 4
// ? gy-4 - is a gap of 4

// ! Fixed NavBar in Bootstrap

// ? fixed-top - The navbar will be fixed at the top of the viewport

// ! ctrl + D - select all the text with the same class name

// ! How to import random Users in the database

// ? https://randomuser.me/api/?results=10

// ? fw-bold - font weight bold

// ! TO ADD A MAP IN THE WEBPAGE

// ? https://www.mapbox.com/
// ? Go to Integrate Mapbox
// ? Select JS Web
// ? Select Use the Mapbox Cdn
// ? Copy the code { Css: TOP } [ Script:bottom ]
// ? Next and Create a new Script and paste the Code

// ! Create a div with the id of map { <div id="map"></div> }

// ? last add the style of the map { <style>#map { #map {width: 100%; height: 100%; border-radius: 10px;} }</style> }
